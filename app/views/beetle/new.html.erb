<h1>Beetle#new</h1>
<%= @error if @error %>

<%= form_for @beetle, url: beetle_new_path do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_field :description %>
  <%= f.submit %>
<% end %>
